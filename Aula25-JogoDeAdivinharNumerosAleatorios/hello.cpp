#include <iostream>

int main() {

    int num;
    int guess;
    int tries;

    srand(time(null));
    num = (rand() % 100) + 1;
    std:: cout << "********** NUMBER GUESSING GAME **********";

    do{
        std::cout << "Enter a guess between (1-100)";
        std::cin >> guess;
    }
    tries++

    if guess()

    return 0;
}